apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: blue-website-application
  namespace: blue
  labels:
    name: blue-website-application
    service: blue-website-application-service
spec:
  replicas: 1
  selector:
    matchLabels:
      name: blue-website-application
      type: frontend
  template:
    metadata:
      labels:
        name: blue-website-application
        type: frontend
    spec:
      containers:
      - name: blue-website-application
        image: nisumpk/website_application
        ports:
        - containerPort: 9001
        env:
        - name: MY_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: SERVER_PORT
          value: -Dserver.port=9001
        - name: DISCOVERY_SERVER_URL
          value: "-Deureka.client.serviceUrl.defaultZone=http://blue-discovery-server-service.blue.svc.cluster.local:7001/eureka/"
        - name: ADMIN_APPLICATION_URL
          value: -Dadmin.application.url=http://216.218.200.224:31007/admin/companies
        - name: NEWS_APPLICATION_URL
          value: -Dnews.application.url=http://216.218.200.224:31007/news/twitter
        command: ["java"]
        args: ["$(SERVER_PORT)", "$(ADMIN_APPLICATION_URL)", "$(NEWS_APPLICATION_URL)", "$(DISCOVERY_SERVER_URL)", "-Deureka.instance.preferIpAddress=true", "-Deureka.instance.ipAddress=$(MY_POD_IP)", "-jar", "/usr/src/website-application-SNAPSHOT.jar"]
---
apiVersion: v1
kind: Service
metadata:
  name: blue-website-application-service
  namespace: blue
  labels:
    name: blue-website-application-service
spec:
  type: NodePort
  ports:
  - port: 9001
    nodePort: 30016
    targetPort: 9001
    protocol: TCP

  selector:
    name: blue-website-application
