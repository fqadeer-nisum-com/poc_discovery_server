apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: green-config-server
  namespace: green
  labels:
    name: green-config-server
    service: green-config-server-service
spec:
  replicas: 1
  selector:
    matchLabels:
      name: green-config-server
      type: backend
  template:
    metadata:
      labels:
        name: green-config-server
        type: backend
    spec:
      containers:
      - name: green-config-server
        image: nisumpk/config_server
        ports:
        - containerPort: 8001
        env:
        - name: MY_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: GIT_CONFIG_DIRECTORY
          value: "-Dspring.cloud.config.server.git.searchPaths=green"
        - name: SERVER_PORT
          value: "-Dserver.port=8001"
        - name: DISCOVERY_SERVER_URL
          value: "-Deureka.client.serviceUrl.defaultZone=http://green-discovery-server-service.green.svc.cluster.local:7001/eureka/"
        command: ["java"]
        args: ["$(SERVER_PORT)", "$(DISCOVERY_SERVER_URL)", "$(GIT_CONFIG_DIRECTORY)", "-Deureka.instance.preferIpAddress=true", "-Deureka.instance.ipAddress=$(MY_POD_IP)", "-jar", "/usr/src/config-server-SNAPSHOT.jar"]
---
apiVersion: v1
kind: Service
metadata:
  name: green-config-server-service
  namespace: green
  labels:
    name: green-config-server-service
spec:
  type: NodePort
  ports:
  - port: 8001
    nodePort: 31002
    targetPort: 8001
    protocol: TCP

  selector:
    name: config-server
