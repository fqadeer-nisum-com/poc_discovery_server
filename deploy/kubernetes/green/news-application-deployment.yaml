apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: green-news-application
  namespace: green
  labels:
    name: green-news-application
    service: green-news-application-service
spec:
  replicas: 1
  selector:
    matchLabels:
      name: green-news-application
      type: frontend
  template:
    metadata:
      labels:
        name: green-news-application
        type: frontend
    spec:
      containers:
      - name: green-news-application
        image: nisumpk/news_application
        ports:
        - containerPort: 9201
        env:
        - name: MY_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: SERVER_PORT
          value: -Dserver.port=9201
        - name: DISCOVERY_SERVER_URL
          value: "-Deureka.client.serviceUrl.defaultZone=http://green-discovery-server-service.green.svc.cluster.local:7001/eureka/"
        - name: BIZ_APPLICATION_URL
          value: "-Dbiz.application.url=http://green-biz-application-service.green.svc.cluster.local:9601"
        command: ["java"]
        args: ["$(SERVER_PORT)", "$(BIZ_APPLICATION_URL)", "$(DISCOVERY_SERVER_URL)",  "-Deureka.instance.preferIpAddress=true", "-Deureka.instance.ipAddress=$(MY_POD_IP)", "-jar", "/usr/src/news-application-SNAPSHOT.jar"]
---
apiVersion: v1
kind: Service
metadata:
  name: green-news-application-service
  namespace: green
  labels:
    name: green-news-application-service
spec:
  type: NodePort
  ports:
  - port: 9201
    nodePort: 31005
    targetPort: 9201
    protocol: TCP

  selector:
    name: green-news-application
