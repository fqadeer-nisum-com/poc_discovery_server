apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: green-discovery-server
  namespace: green
  labels:
    name: green-discovery-server
    service: green-discovery-server-service
spec:
  replicas: 1
  selector:
    matchLabels:
      name: green-discovery-server
      type: backend
  template:
    metadata:
      labels:
        name: green-discovery-server
        type: backend
    spec:
      containers:
      - name: green-discovery-server
        image: nisumpk/discovery_server
        ports:
        - containerPort: 7001
        env:
        - name: SERVER_PORT
          value: -Dserver.port=7001
        command: ["java"]
        args: ["$(SERVER_PORT)", "-Deureka.instance.preferIpAddress=true", "-Deureka.instance.prefer-ip-address=true" , "-jar", "/usr/src/discovery-server-SNAPSHOT.jar"]
---
apiVersion: v1
kind: Service
metadata:
  name: green-discovery-server-service
  namespace: green
  labels:
    name: green-discovery-server-service
spec:
  type: NodePort
  ports:
  - port: 7001
    nodePort: 31001
    targetPort: 7001
    protocol: TCP

  selector:
    name: green-discovery-server
